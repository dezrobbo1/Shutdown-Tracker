# Repository Reset Note

The active repository was reset to this small XML round-trip laboratory because the previous architecture was built before the central Microsoft Project progress transaction was proven.

The reset deliberately removed the backend, database, worker, approval lifecycle, Mobile app, Critical reporting, operational simulation, extensive ADR set and restrictive agent governance from the active tree. The complete pre-reset state remains available through Git history and archive branch `archive/pre-roundtrip-lab-reset-2026-08-27`, created from pre-reset `main` commit `fcb8d7be8f94565c52d99c666cd148b4b523c9cf`.

Code should be recovered from history only when it is independently useful and does not reintroduce the failed field-only transaction assumption.
