# Implementation Status Map

This document is primary product authority for distinguishing approved product direction from behaviour present in the current product-trial foundation. The foundation was established directly from `main`; PR #48 is not its runtime or product-authority base. The opt-in [Tier 1 Project Round-Trip Trial](tier1-project-roundtrip-trial.md) is a bounded browser-local evidence capability, not production persistence or an API/data-contract commitment.

## Status labels

Only these labels are used:

| Label | Meaning |
| --- | --- |
| Verified in repository | A bounded runtime path or independently useful technical capability is present and supported by repository evidence. Limits still apply. |
| Read-only API-wired | The surface reads backend data but does not provide the production write workflow. |
| Static visual only | Frontend structure or empty-state UI exists; it does not establish production behaviour. |
| Designed, not built | Product direction exists without the required end-to-end runtime capability. |
| Explicitly excluded | Outside the active product boundary. |

A document, visual shell, enum, migration table, disabled control, or compatibility component alone does not prove an end-to-end production capability.

## Capability map

| Capability | Status | Repository evidence and limit |
| --- | --- | --- |
| Login visual flow | Static visual only | `apps/console/src/ConsoleViews.tsx` provides the review-only Login transition. It does not establish a session or identity. |
| OIDC and production authorization | Designed, not built | There is no production identity provider integration or Tier 1/Tier 2/Tier 3 authorization enforcement in either client or `services/api`. |
| Projects Home and project switching | Static visual only | `apps/console/src/App.tsx` and `apps/console/src/ConsoleViews.tsx` provide an honest no-project entry state. There is no fabricated project portfolio, project query, or production switching workflow. |
| Project creation | Designed, not built | No production create-project client workflow is wired. Main retains `services/api/src/main/java/com/shutdowntracker/api/project/ReviewProjectBootstrapRunner.java` as a synthetic review utility; it is not project creation authority. |
| Project lifecycle | Designed, not built | `infra/migrations/V002__projects_snapshots_and_imports.sql` has partial status storage, but there is no ordinary frontend dataset, approved lifecycle service, or client write path. |
| Production three-tier membership | Designed, not built | The browser-local Tier 1 round-trip identity is not a production membership, entitlement, identity, or authorization record. Ordinary Mobile no longer fabricates review personas. |
| Production Tier 2-to-Tier 3 direct-report relationships | Designed, not built | There is no production direct-report persistence, API, membership validation, or authorization enforcement. |
| Production Tracker task assignments | Designed, not built | The approved Tier 1-to-Tier 2 and direct-report Tier 2-to-Tier 3 model has no persistence, API, membership, or authorization enforcement. Imported Microsoft Project resource assignments are source facts, not Tracker authority. |
| Today | Verified in repository | The Tier 1 round-trip trial derives a local calendar-day projection and attention state from a temporary imported XML schedule using current browser/device time in the IANA time zone captured at session start. Ordinary Today shows no active-project state; configured production project/site time zone, operational-day settings, DST-disambiguated event instants, and a production query/API remain unbuilt. |
| Tasks explorer | Verified in repository | The Tier 1 round-trip trial browses and searches the imported XML hierarchy, displays source Duration using Project duration units/settings, and treats only executable leaves as tracked tasks. Summary rows remain labelled, non-clickable hierarchy context. Ordinary mode shows no active-project state; there is no production project-task query, schedule editor, or recalculation. |
| Task Dashboard | Verified in repository | The Tier 1 round-trip trial derives imported leaf-task context, Duration, local execution, progress, problems/actions, and history from browser-memory state. Summary hierarchy rows cannot open a tracked-task Dashboard. Ordinary mode has no fabricated task dashboard and there is no production record API. |
| Task execution | Verified in repository | The Tier 1 round-trip trial implements Tier 1 Can't Start, Start, Pause, Resume, Finish, current browser-local whole-minute timestamps, late-start context, pause/problem separation, and field-progress observations for imported executable leaves. “Location” is the device-configured IANA time zone captured at session start, not Project-derived or server-verified; repeated daylight-saving wall-clock minutes fail closed. There is no persisted execution API, audited correction path, production authorization, or offline behaviour. |
| Discussion/comments | Designed, not built | Product placement is defined, but ordinary frontend shells contain no fabricated discussion records and no persistence/API writes exist. |
| Delays / Problems | Verified in repository | The Tier 1 round-trip trial can create, link, and resolve browser-local task problems while keeping adverse delay distinct from a pause interval. No production persistence or API exists. |
| Actions | Verified in repository | The Tier 1 round-trip trial can create, link, and complete browser-local task actions. No production persistence or API exists. |
| Evidence | Designed, not built | Product placement is defined, but ordinary frontend shells contain no fabricated evidence records and no production metadata/upload APIs exist. |
| Mobile offline queue | Designed, not built | Mobile does not fabricate sync/recovery state. There is no IndexedDB queue, service worker replay path, or production submission transport. |
| Retained Critical schema compatibility | Verified in repository | `infra/migrations/V006__critical_watchlists_reporting.sql` contains watchlist/work-pack, reporting policy/version, period, update, and supersession records. Its legacy source and ownership shapes do not implement the approved model. The applied migration remains unchanged as compatibility infrastructure. |
| Approved Critical UI | Designed, not built | Ordinary Console mode shows no active-project state. Imported Project Critical values may appear as read-only round-trip context. No fabricated Critical dataset, configuration/submission runtime, or production Critical API exists. |
| Approved Critical API and persistence | Designed, not built | No approved selected-leaf/work-pack service, Tier 2 reporting-owner workflow, policy-template API, obligation scheduler, submission API, or authorization path exists. A future additive migration must reconcile V006 compatibility with the approved model. |
| Browser MSPDI/XML inspection | Verified in repository | `apps/console/src/projectXmlPreview.ts` validates XML, confirms the Microsoft Project MSPDI namespace, and extracts Project/task identity, hierarchy, Duration/DurationFormat, Project duration-display settings, and other bounded task facts for local read-only inspection. It does not parse `.mpp`, persist an import, or calculate a schedule. |
| Tier 1 browser-local Project round-trip product trial | Verified in repository — frontend/local Tier 1 round-trip product trial | Ordinary Import can inspect a selected XML/MSPDI source losslessly and explicitly start the trial from its retained bytes/text/hash; `VITE_SHUTDOWN_TRACKER_TIER1_ROUNDTRIP_TRIAL=true` remains a direct-entry shortcut. The temporary imported hierarchy displays source Project duration and supports Tier 1 execution only for tracked leaves, timestamped from current device time in the IANA zone captured at session start. Summary rows remain source/hierarchy context and cannot enter operational counts, Dashboard navigation, mappings, or candidate inputs. The trial provides optional reviewed Actual Start/Finish or `% Complete`/`Physical % Complete` mappings and a separate complete-source candidate. The UI guides an external manual Project recalculation and accepts a result XML for conservative local comparison; the repository does not perform or verify that external recalculation. It is browser-only XML/MSPDI evidence with no server-verified/configured site clock, DST-disambiguated absolute event instant, production persistence, native `.mpp`, final export contract, backend candidate workflow, automatic Project operation, or Project recalculation inside Shutdown Tracker. |
| Import source admission validation | Verified in repository | `services/api/src/main/java/com/shutdowntracker/api/sourcefile/SourceFileValidationService.java` checks supported filename/extension, non-empty content, and configured size. This is admission validation, not malware scanning or complete MSPDI semantic validation. |
| Import source storage, hash, and provenance | Verified in repository | `services/api/src/main/java/com/shutdowntracker/api/sourcefile/SourceFileUploadService.java`, `services/api/src/main/java/com/shutdowntracker/api/sourcefile/storage/LocalSourceFileStorage.java`, and `services/api/src/main/java/com/shutdowntracker/api/sourcefile/metadata/SourceFileMetadataService.java` store a versioned upload reference, SHA-256 content hash, and create a pending import batch with audit. Local filesystem storage is development/review infrastructure, not a production object-store implementation. |
| Microsoft Project parse summary | Verified in repository | `services/project-worker/src/main/java/com/shutdowntracker/projectworker/importer/MpxjProjectImportSummaryService.java` uses MPXJ to report bounded project/task/resource/assignment/calendar/custom-field counts without calculating the schedule. It does not return or persist a full task/entity snapshot. |
| Imported entity and snapshot storage | Verified in repository | `infra/migrations/V002__projects_snapshots_and_imports.sql`, `infra/migrations/V003__imported_project_entities.sql`, and `services/api/src/main/java/com/shutdowntracker/api/importedproject` provide versioned snapshot and imported task/resource/assignment/extended-attribute persistence. No connected production parser currently fills the complete model. |
| Immutable snapshot model | Verified in repository | New imports create versioned snapshot identities and imported entity rows; review state changes do not replace source identity. This is repository/application-path evidence, not a claim that arbitrary direct SQL mutation is impossible. |
| Import snapshot review API | Verified in repository | `services/api/src/main/java/com/shutdowntracker/api/importreview/ImportReviewController.java` and `services/api/src/main/java/com/shutdowntracker/api/importreview/ImportReviewService.java` provide snapshot list/detail plus audited accept/reject endpoints. They do not provide the approved mapping, reconciliation, and activation workflow. |
| Ordinary Console configured import review | Read-only API-wired | `apps/console/src/apiReviewClient.ts` and `apps/console/src/ImportExportView.tsx` can read configured snapshot list/detail data. The ordinary Console does not invoke backend import acceptance, rejection, upload, parse, mapping, reconciliation, or activation writes. Its separate local XML action creates only the explicitly labelled browser-memory trial. |
| Task UID/ID lineage | Verified in repository | `services/api/src/main/java/com/shutdowntracker/api/tasklineage` and `packages/api-client/src/index.ts` provide suggested-link creation and accept/reject decisions over stored snapshots. There is no automatic matching engine or ordinary Console lineage UI. |
| Operational Mapping | Designed, not built | Product meaning is documented, but ordinary Project Settings contains no fabricated mapping-health values. No Source Catalogue, profile/category mapping, membership resolution, mapping-health service, API, or production UI write path exists. |
| Retained export-preview compatibility | Verified in repository | Main retains `infra/migrations/V005__approval_and_export_batches.sql`, `services/api/src/main/java/com/shutdowntracker/api/exportpreview`, `packages/project-export-contract`, and `services/project-worker/src/main/java/com/shutdowntracker/projectworker/exporter/MpxjMspdiExportArtifactService.java`. This is experimental technical compatibility only: the writer creates a patch-shaped ProjectFile from selected task fields and is not the approved final export or round-trip design. |
| Ordinary Console Export | Designed, not built | Import / Export marks export as not finalised. The explicit round-trip trial is a separate evidence mode and must not make main's compatibility writer or PR #48's candidate pipeline the required product workflow. |
| PR #48 candidate approval, sealed preview, and browser RoundTripWorkspace | Explicitly excluded | Those changes are not present in the active product foundation and are not prerequisites or active product authority. PR #48 remains a superseded technical/research workstream. |
| Final Microsoft Project export / round trip | Designed, not built | Format, reviewed inputs, comparison, adoption, and verification design will be revisited after the imported-schedule evidence is assessed. There is no required real-human PR #48 manual gate on the active foundation. |
| Native `.mpp` output | Explicitly excluded | Shutdown Tracker does not provide a server-side native `.mpp` writer. The final interchange format and external Microsoft Project interaction are intentionally deferred rather than inferred from the compatibility writer. |
| Scheduler / CPM functionality | Explicitly excluded | Shutdown Tracker does not calculate CPM, critical path, float, resource levelling, dependency consequences, schedule optimisation, or Project recalculation. Imported Project Critical remains read-only context. |

## Interpretation rules

- **Verified in repository** proves only the bounded capability stated in its row.
- Browser MSPDI inspection alone is a local read-only utility; it does not prove upload, persistence, activation, or round-trip acceptance.
- `VITE_SHUTDOWN_TRACKER_TIER1_ROUNDTRIP_TRIAL=true` proves only the bounded browser-local evidence workflow described in its row. It does not prove production persistence, authorization, offline replay, cross-device synchronization, or a settled backend data model. A trial disposition is not production approval, and its experimental mappings do not settle the final export contract.
- Main's source/snapshot/import-review capabilities are independently useful foundations and do not make PR #48 a prerequisite.
- V006 proves retained Critical schema compatibility, not the approved Critical workflow.
- Imported Project resource assignments do not prove Tier 2/Tier 3 operational assignments.
- Main's export-preview and minimal-writer code is experimental compatibility, not current product authority.
- The absence of PR #48's candidate pipeline and RoundTripWorkspace is deliberate; no mandatory manual Microsoft Project acceptance gate is part of the production product path. The separately activated round-trip trial includes an evidence-only manual Project step.
- Product authority defines what should be built; runtime source, migrations, and tests determine what is built.
